import { getCookie } from '../utils'

export default ({ app }) => {
	// let timer = null
	// setUserInfo(app)
	// setToken(app)
	// console.log(app)
	// let whiteList = ['/', '/login', '/register', '/demo', '/forget', '/contract/mobileSign', '/shopadmin', '/admin']
	// app.router.beforeEach((to, from, next) => {
	// 	let access_token = getCookie('access_token')
	// 	if (access_token || whiteList.includes(to.path)) {
	// 		next()
	// 		app.store.commit('CHANGE_LOGIN', true)
	// 	} else {
	// 		next('/login')
	// 		app.store.commit('CHANGE_LOGIN', false)
	// 	}
	// })
}
